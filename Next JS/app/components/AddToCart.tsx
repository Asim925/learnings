"use client";
const AddToCart = () => {
  return (
    <button
      className="btn btn-secondary"
      onClick={() => console.log("clicked")}
    >
      Add to cart
    </button>
  );
};

export default AddToCart;
